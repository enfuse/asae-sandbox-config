spring:
  application:
    name: 'studentsapp'
  config:
    activate:
      on-profile: default

---
spring:
  config:
    activate:
      on-profile: key-vault
  cloud:
    azure:
      keyvault:
        secret:
          enabled: true
          property-source-enabled: true
